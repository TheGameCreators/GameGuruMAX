-- DESCRIPTION: Hides the object from view during the game, and the collision will still be active so you should turn physics off if not needed.

function invisible_init(e)
end

function invisible_main(e)
 Hide(e)
end
